console.log('SALAM from preload js');

/* window.addEventListener('DOMContentLoaded', () => {
    document.querySelector('button').addEventListener('click', () => {
        console.log(process.version);
    });
});
 */

window.test = "SALAM from window test";